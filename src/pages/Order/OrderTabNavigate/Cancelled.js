import React from 'react';
import { View, Text } from 'react-native';

const Cancelled = () => {
    return (
        <View>
            <Text>aaa</Text>
        </View>
    );
};

export default Cancelled;